# CarGo Kenya Backend

This repository contains the backend scaffold for the CarGo Kenya MVP.

## Setup

1. Copy `.env.example` to `.env`.
2. Set `DATABASE_URL` to your PostgreSQL development database.
3. Run `npm install`.
4. Run `npx prisma validate`.
5. Run `npx prisma generate`.
